const db = require('../models');

const create = async (req, res) => {
    // create a function canTodo() that checks credentials of user to see if they have authorization to create a todo.
    // if user is authorized, create the todo.
    // else send an error message.
}